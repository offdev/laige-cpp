# laige-sample

Module per PRD §10.1: reference games — isometric ARPG (flagship),
platformer, lockstep arena, MMO demo zone. Sample game projects live under
the top-level `samples/` directory, each with its own `LICENSE` (ADR 0001);
this directory holds engine-side reference-game code.

Status: not started.
