# laige-core — engine foundation module (PRD §10.1).
#
# M0-REPO-01: directory placeholder only. The laige-core library target
# lands in M0-BUILD-01 (static/shared via LAIGE_BUILD_SHARED) and must be
# passed through laige_apply_engine_policy(); the first code
# (Result/Status) lands in M0-CORE-01.
#
# Dependency rule (PRD §10.1): laige-core depends on nothing internal.
