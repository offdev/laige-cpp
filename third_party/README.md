# third_party/

Reserved placeholder for vendored third-party code that is not tracked by
`deps.lock`. The canonical home for the PRD §11-listed, lock-tracked
dependencies is `deps/` (M0-DEP-01). This directory stays empty until an
ADR places code here.
